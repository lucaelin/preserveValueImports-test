{
  "name": "rollup-plugin-esbuild-preservevalueimports",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "prebuild:tsc": "rm build:tsc/* || true",
    "build:tsc": "tsc",
    "prebuild:rollup": "rm build:rollup/* || true",
    "build:rollup": "rollup -c",
    "prebuild:esbuild": "rm -p build:esbuild/* || true",
    "build:esbuild": "esbuild ./src/* --outdir=./build:esbuild"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^2.70.2",
    "rollup-plugin-esbuild": "^4.9.1"
  },
  "dependencies": {
    "esbuild": "^0.14.36",
    "typescript": "^4.6.3"
  }
}